define(['knockout', 'ais'], function(ko, ais) {

    // implement required data fetches
    function sync() {
        //sync code
    }
    function init() {
        return ['ds is working...']
    }
    function getItems() {
       
    }
    return {
        sync: sync, 
        init: init, 
        getItems: getItems
    }
});